SSTP_SERVER="server"
USERNAME="username"
PASSWORD="password"

# Optional:
# SSTP_IGNORE_CERT=true      # skip server certificate verification (self-signed)
# VPN_DNS=1.1.1.1            # DNS server if the VPN server does not provide one
# SSTP_PPPD_OPTS="refuse-eap noccp"  # extra pppd options
# SSTP_CONNECT_TIMEOUT=60    # seconds to wait for ppp0 to come up
# SSTP_LOG_LEVEL=1           # 0..4
